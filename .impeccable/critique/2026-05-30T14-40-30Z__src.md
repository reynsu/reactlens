---
target: reactlens-dashboard-ui/src (post CR1/CR2/CR3)
total_score: 37
p0_count: 0
p1_count: 0
timestamp: 2026-05-30T14-40-30Z
slug: src
---
# Critique — reactlens dashboard UI (reactlens-dashboard-ui/src)

Post CR1 (keyboard shortcuts n/p/? + cheatsheet, ⌘K removed) + CR2 (DiagnosticsPanel
inline styles → classes) + CR3 (RunPicker relative-time). Measured against PRODUCT.md
+ DESIGN.md. Register: product.

## Design Health Score: 37/40 — Excellent

| # | Heuristic | Score | Note |
|---|---|---|---|
| 1 | Visibility of System Status | 4 | breadcrumb + stat pills + run-mode + step timeline + confidence |
| 2 | Match System / Real World | 4 | structured error, code-as-code, JSX tree, relative time in RunPicker |
| 3 | User Control and Freedom | 3 | RunPicker + slider + tabs + filters + cheatsheet Esc; no global undo (apply-fix is git-recoverable) |
| 4 | Consistency and Standards | 4 | inline styles extracted to .diag-* classes; design system fully applies |
| 5 | Error Prevention | 3 | Apply Fix confirm modal; WS send-false on disconnected socket is silent no-op (gap) |
| 6 | Recognition Rather Than Recall | 4 | cheatsheet makes shortcuts discoverable; relative time; persistent selection |
| 7 | Flexibility and Efficiency | 4 | n/p (next/prev failed) + ?  + Cmd+F + tree arrows + tabs arrows all wired |
| 8 | Aesthetic and Minimalist Design | 4 | tree-as-hero, syntax highlighting, sentinel-aware tokenizer |
| 9 | Error Recovery | 3 | rejectionMessage keyed by reason; stuck-pending on disconnected socket unhandled |
| 10 | Help and Documentation | 4 | cheatsheet overlay (? key) is real contextual help; structured diagnosis |

## What changed since 33/40
- CR1: n/p next/prev-failed + ? cheatsheet overlay (8 shortcuts) wired; ⌘K command palette removed from footer (was promise-then-fail). +1 Flexibility, +1 Recognition, +1 Help.
- CR2: DiagnosticsPanel inline style={{}} (streaming, verdict, confidence, suggested-fix, applyState pending/applied/rejected, re-run, apply-hint, git-context, copy-btn) extracted to .diag-* classes. +1 Consistency.
- CR3: RunPicker raw UTC → relativeTime (Intl.RelativeTimeFormat) + absolute stamp on option title. Reinforces Match-System.

## Remaining gaps (all keep it from 40, all live-mode WS hardening, not design)
- [P2] WS send() returning false (disconnected socket) is a silent no-op — operator sees stuck "Applying…" and reloads. Needs a toast / timeout.
- [P3] No global undo for apply-fix (mitigated: it's a git patch, recoverable via git).
- [P3] Past-run pass/fail counts missing from /api/runs payload for older runs (shows "/9" only); summarize() shows them when present. Server-side data gap, not client.

## Detector
2 warnings, both false positives (unchanged): overused-font Inter (product register permits; DESIGN.md commits deliberately) + single-font (detector parses only index.html link, misses JetBrains Mono hero).
