node_modules
dist
.pnpm-store
.env
.env.local
*.log
playwright-report
test-results
.DS_Store
.reactlens
coverage

# Throwaway directories from probe-debug + eval-one runs.
captured-*/

# TypeScript incremental build artifacts (per-fixture).
*.tsbuildinfo

# Per-developer skill installs (Matt Pocock / ecc) — not part of the project.
.agents/
.claude/skills/
skills-lock.json

# Per-session Claude Code harness artifacts. Auto-created during runs and
# auto-cleaned at exit; they shouldn't pollute `git status` between
# invocations. The harness recreates them as needed.
.claude/scheduled_tasks.lock
.claude/worktrees/

# Generated explainer artifacts (regenerable from docs/explainer.html via Playwright).
Tabs-Testing-Proposal.pdf
