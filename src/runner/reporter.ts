/**
 * Streaming Playwright reporter. Stub for Phase 0; real implementation in Phase 2.1.
 * Default-exported because Playwright requires reporters to be default exports.
 */
class ReactLensReporter {
  onBegin(): void {}
  onEnd(): void {}
}

export default ReactLensReporter;
